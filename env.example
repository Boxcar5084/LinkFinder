# ElectrumX Configuration
# Copy this file to .env and update with your ElectrumX server details

# ElectrumX server host (IP or hostname)
ELECTRUMX_HOST=100.94.34.56

# ElectrumX server port
# 50001 = TCP (unencrypted)
# 50002 = SSL (encrypted)
ELECTRUMX_PORT=50001

# Use SSL connection (true/false)
# Set to true if using port 50002
ELECTRUMX_USE_SSL=false

# Optional: Path to SSL certificate file for verification
# Leave empty to disable certificate verification (not recommended for production)
ELECTRUMX_CERT=

# Enable verbose debug logging for ElectrumX (true/false)
# When false, hides "Sending request" and "Received first chunk" messages
ELECTRUMX_DEBUG=false

# Default API provider
# Options: "electrumx", "mempool", "blockchain"
DEFAULT_API=mempool

# Mempool.space API Key (optional)
# Get an API key from mempool.space for higher rate limits
# Leave empty to use public rate limits
MEMPOOL_API_KEY=

# SSH Configuration for ElectrumX log access
# SSH server hostname/IP (may differ from ELECTRUMX_HOST)
SSH_HOST=

# SSH username
SSH_USER=

# Optional: Path to SSH private key file
# Leave empty to use default SSH keys (~/.ssh/id_rsa or ~/.ssh/id_ed25519)
SSH_KEY_PATH=

# SSH port (default: 22)
SSH_PORT=22

# Docker container name for ElectrumX (default: electrumx)
ELECTRUMX_DOCKER_CONTAINER=electrumx
